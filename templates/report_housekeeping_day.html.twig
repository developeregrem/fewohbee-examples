<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: Arial, sans-serif; font-size: 12px; }
        table { width: 100%; border-collapse: collapse; }
        th, td { border: 1px solid #000; padding: 6px; vertical-align: top; }
        th { background: #f2f2f2; text-align: left; }
        .muted { color: #666; font-size: 11px; }
    </style>
</head>
<body>
    <div class="header"><div class="muted" style="text-align: center; color: #666; font-size: 11px;">[[ simple.meta.periodLabel ]]<span data-repeat="simple.meta.occupancyTypeLabelKeys" data-repeat-as="type"> | [[ occupancyLabels[type]|trans({}, 'Housekeeping') ]]</span> | [[ simple.meta.subsidiaryName ?: (simple.meta.subsidiaryAllLabelKey|trans({}, 'Housekeeping')) ]] | Erzeugt [[ simple.meta.generatedAt ]]</div></div>
    <div class="footer"><div class="muted" style="text-align: right; font-size: 10px;">Seite {PAGENO} von {nbpg}</div></div>

    <h2>[[ 'housekeeping.title'|trans({}, 'Housekeeping') ]] ([[ simple.views.housekeepingDay.date|date('d.m.Y') ]])</h2>

    <table>
        <thead>
        <tr>
            <th>[[ 'housekeeping.room'|trans({}, 'Housekeeping') ]]</th>
            <th>[[ 'housekeeping.occupancy'|trans({}, 'Housekeeping') ]]</th>
            <th>[[ 'housekeeping.guests'|trans({}, 'Housekeeping') ]]</th>
            <th>[[ 'housekeeping.reservation'|trans({}, 'Housekeeping') ]]</th>
            <th>[[ 'housekeeping.status'|trans({}, 'Housekeeping') ]]</th>
            <th>[[ 'housekeeping.assigned_to'|trans({}, 'Housekeeping') ]]</th>
            <th style="width: 20%">[[ 'housekeeping.note'|trans({}, 'Housekeeping') ]]</th>
        </tr>
        </thead>
        <tbody>
        <tr data-repeat="simple.views.housekeepingDay.rows" data-repeat-as="row">
            <td>[[ row.room ]]</td>
            <td>[[ row.occupancyLabelKey|trans({}, 'Housekeeping') ]]</td>
            <td>[[ row.guests ]]</td>
            <td>[[ row.reservationSummary ]]</td>
            <td>[[ row.statusLabelKey ? (row.statusLabelKey|trans({}, 'Housekeeping')) : '' ]]</td>
            <td>[[ row.assignedTo ]]</td>
            <td>[[ row.note ]]</td>
        </tr>
        </tbody>
    </table>
</body>
</html>
